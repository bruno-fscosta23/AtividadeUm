<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.sp.senac.atividadeum">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Projeto_Peixe_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Exer_Logo">

        </activity>
        <activity android:name=".App_Sign"></activity>
        <activity android:name=".inter_dois" />
        <activity android:name=".projetoLogin_pag" />
        <activity android:name=".projetoLogin" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>